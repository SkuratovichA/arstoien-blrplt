<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>[Admin] Požadavek na ověření dokumentů</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #dc2626;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .content {
      background-color: #f9fafb;
      padding: 30px;
      border: 1px solid #e5e7eb;
    }
    .alert {
      background-color: #fef2f2;
      padding: 15px;
      border-radius: 6px;
      margin: 20px 0;
      border-left: 4px solid #dc2626;
    }
    .user-details {
      background-color: white;
      padding: 15px;
      border-radius: 6px;
      margin: 20px 0;
      border: 1px solid #e5e7eb;
    }
    .reason-box {
      background-color: #fffbeb;
      padding: 15px;
      border-radius: 6px;
      margin: 20px 0;
      border-left: 4px solid #f59e0b;
    }
    .button {
      display: inline-block;
      background-color: #7c3aed;
      color: white;
      padding: 12px 24px;
      text-decoration: none;
      border-radius: 6px;
      margin-top: 20px;
      font-weight: bold;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
      font-size: 12px;
      color: #6b7280;
    }
    ul {
      padding-left: 20px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>📋 Požadavek na ověření dokumentů</h1>
  </div>

  <div class="content">
    <p>Dobrý den,</p>

    <p>Administrátor požádal o manuální ověření dokumentů pro následujícího uživatele:</p>

    <div class="alert">
      <p><strong>⚠️ Akce požadována:</strong> Zkontrolujte prosím dokumenty uživatele a ověřte jeho podnikatelský status.</p>
    </div>

    <div class="user-details">
      <p><strong>Údaje uživatele:</strong></p>
      <ul>
        <li><strong>Jméno:</strong> {{firstName}} {{lastName}}</li>
        <li><strong>E-mail:</strong> {{email}}</li>
        {{#if phone}}
        <li><strong>Telefon:</strong> {{phone}}</li>
        {{/if}}
        {{#if companyPhone}}
        <li><strong>Telefon firmy:</strong> {{companyPhone}}</li>
        {{/if}}
        {{#if ico}}
        <li><strong>IČO:</strong> {{ico}}</li>
        {{/if}}
        <li><strong>Datum registrace:</strong> {{registrationDate}}</li>
      </ul>
    </div>

    <div class="reason-box">
      <p><strong>Důvod požadavku:</strong></p>
      <p>{{reason}}</p>
      {{#if aresError}}
      <p><strong>Chyba ARES:</strong> {{aresError}}</p>
      {{/if}}
    </div>

    <p><strong>Co je potřeba ověřit:</strong></p>
    <ul>
      <li>Platný výpis z obchodního rejstříku nebo živnostenský list</li>
      <li>Ověření podnikatelského oprávnění</li>
      <li>Kontaktní údaje společnosti</li>
      {{#if missingIco}}
      <li>IČO společnosti (nebylo poskytnuto při registraci)</li>
      {{/if}}
    </ul>

    <p>Po ověření dokumentů můžete uživatele schválit v administračním panelu.</p>

    <a href="{{adminUrl}}" class="button">Otevřít administraci</a>

    <p style="margin-top: 30px;">
      <strong>Admin Panel - Aukce Autobusů</strong>
    </p>
  </div>

  <div class="footer">
    <p>Tento e-mail byl odeslán automaticky systémem.</p>
    <p>&copy; 2025 Aukce Autobusů. Všechna práva vyhrazena.</p>
  </div>
</body>
</html>
